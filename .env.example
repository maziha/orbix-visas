# Server-only (do not prefix with VITE_)
RESEND_API_KEY=re_your_api_key_here
# Optional — defaults to CONTACT_EMAIL in src/lib/contact-info.ts
ENQUIRY_TO_EMAIL=you@example.com
# Optional — defaults to OrbiX + orbixvisas@resend.dev in send-enquiry-email.ts
RESEND_FROM_EMAIL=Your Name <onboarding@resend.dev>

# Netlify: set only RESEND_API_KEY (required). Redeploy after saving.
